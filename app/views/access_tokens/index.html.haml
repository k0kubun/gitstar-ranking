%h3
  Access Tokens (#{AccessToken.count})

%table.table
  %thead
    %tr
      %th User
      %th Created At
  %tbody
    - @access_tokens.each do |access_token|
      %tr
        %td= access_token.user.login
        %td= access_token.created_at

= paginate @access_tokens
